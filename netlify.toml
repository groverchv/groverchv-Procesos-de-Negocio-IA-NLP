# Netlify configuration for Angular SPA deployment

[build]
  base = "Frontend"
  command = "npm install && npx ng build --configuration production"
  publish = "dist/frontend/browser"

# SPA fallback — redirect all routes to index.html for Angular routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
